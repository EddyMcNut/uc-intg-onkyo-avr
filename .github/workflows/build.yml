name: Build and Release

on:
  push:
    branches:
      - "**"
    paths:
      - "**/*.js"
      - "**/*.ts"
      - ".github/**/*.yml"
      - "package-lock.json"
    tags:
      - "v*.*.*"
  pull_request:
    branches: ["main"]
    types: [opened, synchronize, reopened]

permissions:
  contents: write

env:
  INTG_NAME: onkyo_avr

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      artifact-name: ${{ steps.set-artifact.outputs.artifact-name }}
      version: ${{ steps.pkg.outputs.version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Read version from package.json
        id: pkg
        run: |
          VERSION=$(jq -r .version package.json)
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Sync driver.json version with package.json
        run: |
          jq --arg ver "$VERSION" '.version = $ver' driver.json > driver.json.tmp && mv driver.json.tmp driver.json

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"

      - name: Build
        run: |
          npm install
          npm run build

      - name: Remove config.json
        run: rm -f config.json

      - name: Get production modules
        run: |
          rm -Rf node_modules
          npm install --production

      - name: Add version
        run: |
          mkdir -p artifacts
          cd artifacts
          echo ${{ env.VERSION }} > version.txt

      - name: Create custom intg archive
        id: set-artifact
        shell: bash
        run: |
          mv dist artifacts/
          mv artifacts/dist artifacts/bin
          mv artifacts/bin/index.js artifacts/bin/driver.js
          cp package.json artifacts/bin/
          mv node_modules artifacts/bin/
          cp driver.json artifacts/
          # patch metadata to not conflict with pre-installed driver
          jq '.driver_id = "onkyo_avr_custom_driver" | .name.en = "Onkyo AVR custom"' driver.json > artifacts/driver.json
          ARTIFACT_NAME="uc-intg-${{ env.INTG_NAME }}-${{ env.VERSION }}"
          echo "ARTIFACT_NAME=$ARTIFACT_NAME" >> $GITHUB_ENV
          echo "artifact-name=$ARTIFACT_NAME" >> $GITHUB_OUTPUT

      - name: Set driver.js as executable
        run: chmod +x artifacts/bin/driver.js

      - name: Create upload artifact
        shell: bash
        run: |
          tar czvf ${{ env.ARTIFACT_NAME }}.tar.gz -C ${GITHUB_WORKSPACE}/artifacts .
          ls -lah

      - uses: actions/upload-artifact@v4
        id: upload_artifact
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: ${{ env.ARTIFACT_NAME }}.tar.gz
          if-no-files-found: error
          retention-days: 3

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.build.outputs.artifact-name }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ needs.build.outputs.artifact-name }}.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
